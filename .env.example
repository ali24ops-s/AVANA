# Root environment variables are not required in PR 3.
# Use only synthetic local values in ignored .env files.
# API configuration is introduced in PR 5.
# Local PostgreSQL and Redis configuration is introduced in PR 6.
